meta {
  name: Publish a product
  type: http
  seq: 5
}

patch {
  url: {{credytApiUrl}}/products/{{productId}}/1
  body: json
  auth: inherit
}

body:json {
  {
    "publish": true
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
